# projects

이 폴더 아래에 `projects/{GITHUB_ID}/`를 만들고, 필수 파일인 `IDEA.md`와 `app.py`를 넣습니다.

결과를 더 보여 주고 싶다면 선택으로 짧은 `README.md`와 `output/` 폴더를 추가할 수 있습니다. `output/`에는 API Key가 보이지 않는 결과 화면 스크린샷만 1~2장 넣습니다.
