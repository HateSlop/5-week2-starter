# CLI 개선 작업

## 목표

한 번 질문하고 종료되는 `main.py`를 여러 질문을 이어서 입력할 수 있는 CLI로 개선합니다.

## 요구사항

- 사용자가 여러 질문을 연속으로 입력할 수 있어야 합니다.
- `/exit`를 입력하면 API를 호출하지 않고 정상 종료해야 합니다.
- 빈 입력은 API를 호출하지 않고 다시 입력받아야 합니다.
- 기존 OpenAI Responses API 호출 구조를 유지해야 합니다.
- `README.md`에 반복 입력과 종료 방법을 추가해야 합니다.

## 지켜야 할 사항

- API Key를 코드에 직접 넣거나 화면에 출력하지 않습니다.
- 실제 `.env` 내용을 읽거나 수정하지 않습니다.
- 요청하지 않은 웹 프레임워크나 패키지를 추가하지 않습니다.
- 수정 전에는 구현 계획을 먼저 제시합니다.
- 승인받은 파일만 작은 범위로 수정합니다.

## 확인 방법

```bash
python -m py_compile main.py
python main.py
git diff
git status --short
```

일반 질문 두 번, 빈 입력, `/exit`를 차례로 확인합니다.

## 완료 기준

- 질문을 두 번 이상 연속으로 입력할 수 있습니다.
- 빈 입력에서는 API를 호출하지 않습니다.
- `/exit`로 오류 없이 종료됩니다.
- `README.md`의 설명과 실제 동작이 일치합니다.
- `.env`가 Git 변경 목록에 나타나지 않습니다.
