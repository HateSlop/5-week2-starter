# AI 아이디어 구체화 도우미

## 목표

사용자가 한 줄로 적은 서비스 아이디어를 바탕으로, AI가 문제·대상 사용자·MVP 기능·첫 행동을 정리해 주는 작은 웹 프로토타입을 만듭니다.

## 요구사항

- `app.py`에 Streamlit 화면을 만듭니다.
- 서비스 아이디어를 적을 수 있는 입력 영역과 실행 버튼이 있어야 합니다.
- 빈 입력에서는 API를 호출하지 않고, 아이디어를 입력하라는 안내를 보여 줍니다.
- OpenAI Responses API로 아래 네 항목을 Markdown 형식으로 정리해 출력합니다.
  1. 해결하려는 문제
  2. 대상 사용자
  3. MVP 핵심 기능 3개
  4. 지금 바로 할 첫 행동
- `README.md`에 실행 방법과 입력 예시를 추가합니다.

## 지켜야 할 사항

- API Key를 코드에 직접 넣거나 화면에 출력하지 않습니다.
- 실제 `.env` 내용을 읽거나 수정하지 않습니다.
- `OPENAI_API_KEY`와 `OPENAI_MODEL`은 환경변수에서 읽습니다.
- Streamlit 이외의 웹 프레임워크, 데이터베이스, 로그인, 배포 기능, MCP·외부 Tool 연동은 추가하지 않습니다.
- 수정 전에는 구현 계획을 먼저 제시합니다.
- 계획에 없는 큰 범위의 기능은 추가하지 않습니다.

## 확인 방법

```bash
streamlit run app.py
git diff
git status --short
```

짧은 아이디어를 입력해 네 항목이 출력되는지 확인하고, 빈 입력에서는 API 호출 없이 안내만 나오는지 확인합니다.

## 완료 기준

- `streamlit run app.py`로 화면이 열립니다.
- 아이디어 입력 후 네 항목의 AI 응답이 출력됩니다.
- 빈 입력에서는 API를 호출하지 않습니다.
- `README.md`의 실행 방법과 실제 동작이 일치합니다.
- `.env`가 Git 변경 목록에 나타나지 않습니다.
